"""
搜索框匹配逻辑全面审计报告

==============================
P0 - 严重问题（已修复）
==============================

【问题1】ST / st 搜索返回拼音匹配的普通股票，而非 ST 股票列表
  实测: 输入 'ST' → 返回 深天马A、韶能股份 等拼音简拼含 'st' 的普通股票
  根因: 
    (a) 'st' 被路由到 _search_by_pinyin，拼音简拼 'st' 匹配了 stm/sht 等普通股票
    (b) 'ST' 大写走 else 分支，同样被 .lower() 后走拼音匹配
    (c) _search_st_pinyin 有 'not matches' 守卫，拼音已有结果时不触发
  修复: 
    (a) 移除 'not matches' 守卫，_search_st_pinyin 始终对 ST 前缀查询触发
    (b) 新增纯 ST 前缀匹配（st/sst/s*st → 返回所有 ST 股票，得分 58+）
    (c) ST 拼音简拼匹配得分降至 45+，低于普通拼音简拼(50+)，避免干扰
  影响: 用户无法通过输入 ST/st 来筛选 ST 股票，严重违反直觉

【问题2】_search_st_pinyin 的 'not matches' 守卫过于严格
  实测: 输入 'st' 时，拼音搜索已匹配到普通股票，ST 匹配被跳过
  根因: 守卫条件 `if not matches` 阻止了 ST 匹配
  修复: 移除守卫，改为始终触发但对 ST 匹配使用较低得分
  影响: 只有拼音搜索无结果时才会尝试 ST 匹配，大部分情况不生效

【问题3】名称中带空格的 trigram 索引污染
  实测: "生 意 宝" 的 trigram 包含 "生 "、" 意"、"意 "、" 宝" 等含空格的 2-gram
  根因: trigram 切分使用了原始 name（含空格），而非 name_clean
  修复: 改用 name_clean 进行 trigram 切分
  影响: 含空格的 2-gram 在中文搜索中几乎不会命中，浪费索引空间

==============================
P1 - 中等问题（已修复）
==============================

【问题4】大写字母查询走不同代码路径
  实测: 'GZMT' vs 'gzmt' 目前结果相同，但路径不同
  'GZMT' → is_lower_ascii=False → else 分支 → _search_by_pinyin('.lower()')
  'gzmt' → is_lower_ascii=True → _search_by_pinyin 分支
  根因: 路由逻辑仅检查 is_lower_ascii，大写字母被归入 'else'
  风险: 当 _search_by_pinyin 内部逻辑变化时，两个路径可能产生不同结果
  当前状态: 由于 .lower() 统一处理，目前结果一致，但逻辑脆弱

【问题5】大小写混合查询的 ST 前缀匹配
  实测: 'St' → is_lower_ascii=False → else 分支 → _search_by_pinyin
  修复: _search_st_pinyin 始终使用 .lower() 处理，大小写兼容

==============================
P2 - 低优先级问题（未修复，建议评估）
==============================

【问题6】maotai 全拼无法搜到贵州茅台
  实测: 搜索 'maotai' 返回 0 结果
  根因: 贵州茅台的全拼是 'guizhoumaotai'，Trie 仅支持前缀匹配
  建议: 增加全拼子串匹配（如 trigram 索引辅助），但会增加误匹配
  影响: 用户输入部分全拼（非前缀）时无法匹配，如 'maotai'、'zhou' 等

【问题7】代码+中文混合查询无结果
  实测: '600茅' 返回 0 结果
  根因: '600茅' 中 '600' 不是纯数字（含中文），'茅' 触发 _search_by_name
       但名称中没有 '600茅' 这个前缀
  建议: 将混合查询拆分为数字部分和中文部分分别匹配后取交集
  影响: 用户在搜索框中同时输入代码片段和名称片段时无法匹配

【问题8】单星号 * 搜索的偶然正确性
  实测: '*' 搜索返回 5 条 *ST 股票
  根因: name_trie 中有 '*' 字符节点（来自 *ST 开头名称），搜索 '*' 前缀匹配
  风险: 依赖实现细节，如果未来 Trie 结构变化可能失效
  建议: 显式处理 '*' 为"返回所有 *ST 股票"

【问题9】** 双星号无结果
  实测: '**' 返回 0 结果
  根因: name_trie 中没有 '**' 前缀
  建议: 归一化处理，将连续星号合并为单个 '*'

【问题10】600519abc 混合查询无结果
  实测: 代码+字母后缀混合，0 结果
  根因: 非纯数字也非纯拼音，走 else 分支，所有子搜索均无匹配
  建议: 提取数字部分做代码前缀搜索，或忽略非字母数字字符

【问题11】600茅 等混合查询无结果
  实测: 代码前缀+中文，0 结果
  根因: 同上，混合查询走 else 分支无匹配
  影响: 用户输入 '600茅' 可能期望搜到 600 开头的茅台相关股票

==============================
已确认正确的行为
==============================

✓ 空输入 → 返回 []（正确）
✓ 纯空格 → 返回 []（正确）
✓ 纯数字代码精确匹配 → 得分 100（正确）
✓ 纯数字代码前缀匹配 → 得分 80（正确）
✓ 中文名称前缀匹配 → 得分 90+（正确）
✓ 中文 trigram 匹配 → 得分 70+（正确）
✓ 拼音全拼前缀匹配 → 得分 60+（正确）
✓ 拼音简拼前缀匹配 → 得分 50+（正确）
✓ 拼音首字母匹配 → 得分 40~43（正确）
✓ *st 只匹配 *ST 前缀（正确）
✓ *stbs 匹配 *ST宝实（正确）
✓ syb 匹配 生 意 宝（正确）
✓ ST/st 搜索返回 ST 股票（已修复）
✓ stjl 匹配 ST京蓝（已修复）
✓ 大小写拼音搜索一致（正确）
✓ 名称空格不影响中文搜索（已修复 trigram）